# Results

Small, diffable results live here (e.g. per-cohort Spearman summaries).

Large outputs are **not** committed. The full scored table for the TAUSO oligo
dataset (`data_scored_pfred_final.csv`, ~45 MB) is archived on Zenodo — see the
top-level README for the DOI. Regenerate any of it from `../pfred_runner.py`.
